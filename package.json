{
  "name": "onlylearns-timer-vercel",
  "version": "1.0.0",
  "description": "Timer app with Vercel and Supabase",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.x.x", // Neueste Version verwenden
    "dotenv": "^16.x.x", // Für Umgebungsvariablen
    "cors": "^2.8.5", // Falls du CORS in den Funktionen brauchst, aber Vercel proxyed
    "express": "^4.18.2" // Falls du Express-Features in der API nutzt
  }
}